<?php


/**********************************************************************************/
/*** THIS FILE HAS BEEN AUTOMATICALLY GENERATED BY THE PANICKAPPS API GENERATOR ***/

/*                It is HIGHLY suggested that you do not edit this file.          */

//  DATABASE:     Nicos
//  FILE:         userlevels.php
//  TABLE:        userlevels
//  DATETIME:     2018-01-24 12:54:16am
//  DESCRIPTION:  N/A

/**********************************************************************************/
			

class Userlevels implements JsonSerializable {

	//-------------------- Attributes --------------------

    private $UserLevelID;
    private $UserLevelName;

	//-------------------- Constructor --------------------

    public function __construct(
		$UserLevelID, 
		$UserLevelName
		) {
        $this->UserLevelID = $UserLevelID;
		$this->UserLevelName = $UserLevelName;
    }

	//-------------------- Reflectors --------------------

	public static $allFields = array(
		"UserLevelID", 
		"UserLevelName"
	);

	public static $hasUniqueFields = true;

	//-------------------- Getter Methods --------------------

	/**
     * @return int(10) unsigned
     */
     public function getUserLevelID() { return $this->UserLevelID; }

	/**
     * @return varchar(255)
     */
     public function getUserLevelName() { return $this->UserLevelName; }


    /**
     * @return int(10) unsigned
     */
     public function getID() { return $this->UserLevelID; }


	//-------------------- Setter Methods --------------------

	/**
     * @param $value varchar(255)
     */
     public function setUserLevelName($value) { $this->UserLevelName = $value; }


	//-------------------- Static (Database) Methods --------------------

            
            
    /**
     * Creates a database entry with the given object's data.
     * @param $t1_object Userlevels
     * @return bool
     */
    public static function create($userlevels_object) {
        $conn = dbLogin();
        $sql = "INSERT INTO userlevels (UserLevelName) VALUES (\"" . $userlevels_object->UserLevelName . "\")";
        if ($conn->query($sql) === TRUE) return true;
        else return false;
    }


     /**
     * Retrieves a database entry matching the ID value provided.
     * @param $id int(10) unsigned
     * @return bool|Userlevels
     */
    public static function getByID($id) {
        $conn = dbLogin();
        $sql = "SELECT * FROM userlevels WHERE UserLevelID = " . $id;
        $result = $conn->query($sql);
        $sqlRowItemAsAssocArray = null;
        if ($result->num_rows > 0) {
            $sqlRowItemAsAssocArray = $result->fetch_assoc();
            $object = new Userlevels(
				$sqlRowItemAsAssocArray["UserLevelID"], 
				$sqlRowItemAsAssocArray["UserLevelName"]);
            return $object;
        }
        else return false;
    }


    /**
     * Returns a database entry matching the unique field value provided.
     * @param $indexValue varchar(255)
     * @return bool|Userlevels
     */
    public static function getByUserLevelName($indexValue) {
        $conn = dbLogin();
        $sql = "SELECT * FROM userlevels WHERE UserLevelName = '" . $indexValue . "'";
        $result = $conn->query($sql);
        $sqlRowItemAsAssocArray = null;
        if ($result->num_rows > 0) {
            $sqlRowItemAsAssocArray = $result->fetch_assoc();
            $object = new Userlevels(
				$sqlRowItemAsAssocArray["UserLevelID"], 
				$sqlRowItemAsAssocArray["UserLevelName"]);
            return $object;
        }
        else return false;
    }


    /**
     * Retrieves all entries or up to a specified limit from the database. Use 0 or negative values as limit to retrieve all entries.
     * @param $limit int
     * @return array|bool
     */
    public static function getMultiple($limit) {
        $conn = dbLogin();
        $sql = "SELECT * FROM userlevels";
        if ($limit > 0) $sql .= " LIMIT " . $limit;
        $result = $conn->query($sql);
        $itemsArray = array();
        if ($result->num_rows > 0) {
            if ($result->num_rows > 0) {
                while($row = $result->fetch_assoc()) {
                    $object = new Userlevels(
				$row["UserLevelID"], 
				$row["UserLevelName"]);
                    array_push($itemsArray, $object);
                }
            }
            return $itemsArray;
        }
        return false;
    }


    /**
     * Updates a database entry with the given object's data.
     * @param $t1_object Userlevels
     * @return bool
     */
    public static function update($userlevels_object) {
        $conn = dbLogin();
        $sql = "UPDATE userlevels SET UserLevelName = \"" . $userlevels_object->getUserLevelName() . "\" WHERE UserLevelID = " . $userlevels_object->getUserLevelID();
        $conn->query($sql);
        if ($conn->affected_rows > 0) return true;
        else return false;
    }


    /**
     * Deletes an entry from the database given the object's data.
     * @param $t1_object Userlevels
     * @return bool
     */
    public static function delete($userlevels_object) {
        $conn = dbLogin();
        $sql = "DELETE FROM userlevels WHERE UserLevelID = " . $userlevels_object->getUserLevelID();
        if ($conn->query($sql) === TRUE) return true;
        else return false;
    }


    /**
     * Returns the number of entries in the database.
     * @return int
     */
    public static function getSize() {
        $conn = dbLogin();
        $sql = "SELECT COUNT(UserLevelID) FROM userlevels";
        $result = $conn->query($sql);
        return $result->fetch_array()[0];
    }


    /**
     * Returns true if the database is empty or false otherwise.
     * @return bool
     */
    public static function isEmpty() {
        $conn = dbLogin();
        $sql = "SELECT COUNT(UserLevelID) FROM userlevels";
        $result = $conn->query($sql);
        return ($result->fetch_array()[0] == 0);
    }


    /**
     * Searches the database for matches in the given field-value pairs in the given associative array.
     * The array should be in the format FieldName -> ValueToSearch where both values are strings.
     * @return array|bool
     */
    public static function searchByFields($fieldsAndValuesAssocArray) {
        $conn = dbLogin();
        
        $combinedWhereClause = "";
        
        foreach ($fieldsAndValuesAssocArray as $field => $value) {
            $combinedWhereClause .= $field . " = \"" . $value . "\" AND ";
        }
        $combinedWhereClause = substr($combinedWhereClause, 0, strlen($combinedWhereClause) - 4);
        
        $sql = "SELECT * FROM userlevels WHERE " . $combinedWhereClause;
        $result = $conn->query($sql);
        if (!$result) return false;
        $itemsArray = array();
        while ($row = $result->fetch_assoc()) {
            $object = new Userlevels(
				$row["UserLevelID"], 
				$row["UserLevelName"]);
            array_push($itemsArray, $object);
        }
        return $itemsArray;
    }

	//-------------------- JSON Generation Methods --------------------

    /**
     * Specifies how objects of this class should be converted to JSON format.
     * @return string
     */
    public function jsonSerialize() {
        $jsonStr = "{\"UserLevelID\": $this->UserLevelID, \"UserLevelName\": \"" . $this->UserLevelName. "\" }";
        return $jsonStr;
    }
    
    /**
     * Converts an array of Userlevels objects to a JSON Array.
     * @param $Userlevels_array Userlevels Array
     * @return bool|string
     */
    public static function toJSONArray($Userlevels_array) {
        $strArray = "[ ";
        foreach ($Userlevels_array as $i) {
            $strArray .= $i->jsonSerialize() . ", ";
        }
        $strArray = substr($strArray, 0, strlen($strArray) - 3);
        $strArray .= "} ] ";
        return $strArray;
    }

}

?>